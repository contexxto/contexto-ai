# Dependencias SOLO de desarrollo/test (no se instalan en Render).
# Uso:  python -m pip install -r requirements-dev.txt
-r requirements.txt

# La suite importa scripts/foso_pois_spike.py (tests/test_overture_release.py, ocho
# pruebas sobre que release de Overture se elige), y ese script usa duckdb, requests y
# psycopg. Render sigue SIN instalarlos: Render lee requirements.txt, no este archivo.
# Pero CI y cualquiera que corra pytest si los necesitan.
#
# Sin esta linea el gate se cae con ModuleNotFoundError en la recoleccion, no en una
# prueba. Paso en la segunda corrida del PR #119: en el portatil del fundador duckdb
# estaba en el .venv de antes y no se notaba.
-r requirements-pois.txt

pytest==8.3.4
pytest-asyncio==0.25.2

# El arnes TLS de R2C1 (tests/arnes_postgres_tls.py) emite la CA y el certificado de
# servidor del PostgreSQL efimero contra el que corren T1/T2/T3. Ya venia instalada de
# forma transitiva por pyjwt[crypto], pero una prueba de SEGURIDAD no deberia depender de
# lo que resuelva otro paquete: si pyjwt cambiara de extras, T1/T2/T3 dejarian de poder
# ejecutarse y el fallo apareceria como un error de import, no como lo que es.
#
# Va aqui y NO en requirements.txt a proposito: Render no la instala por esta via y el
# runtime de produccion no la necesita. Sin pin exacto para no pelearse con lo que resuelva
# pyjwt[crypto]==2.10.1, que exige >=3.4.0.
cryptography>=42
